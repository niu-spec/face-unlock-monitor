# Django core
django~=4.2.0
djangorestframework~=3.14.0

# JWT
djangorestframework-simplejwt~=5.3.0

# Swagger / OpenAPI (drf_yasg in config/urls.py)
drf-yasg~=1.21.7

# CORS
django-cors-headers~=4.3.0

# MySQL
pymysql~=1.1.0
cryptography~=44.0.0

# Production
gunicorn~=21.2.0

# AI dependencies
# dlib must be installed via conda first: conda env create -f environment.yml
# Or run: ..\scripts\setup_backend.ps1 from project root
opencv-python~=4.8.0
numpy~=1.26.0
pillow~=10.0.0
ultralytics~=8.2.0    # D-李东礼：YOLOv8 行人检测（自动下载 yolov8n.pt）

# Face recognition (requires conda dlib on Windows)
face-recognition~=1.3.0
setuptools<81
