# 实时视频分析监测系统 — 总体架构说明

> **项目名称**：home-camera-monitor  
> **应用场景**：居家智能摄像头监控  
> **版本**：v1.5  
> **编制日期**：2026-07-12  
> **技术方案**：Vue3 + Django 4.2（业务 + AI 同进程）+ OpenCV/dlib + MediaMTX + MySQL

---

## 1. 文档目的

本文档描述居家摄像头监控系统的总体架构、技术选型、模块划分、数据流与部署方案，供立项评审、中期检查与结题验收使用。

---

## 2. 项目背景与目标

### 2.1 背景

现代家庭对居家安全与成员看护的需求日益增长。本系统基于家庭摄像头实时视频流，结合人脸识别与计算机视觉分析，实现对家庭成员的识别、在家人数统计、陌生人检测，以及厨房等危险区域的闯入告警，并对积水、着火、人员摔倒等异常情况进行自动识别与告警。

### 2.2 建设目标

| 目标 | 说明 |
|------|------|
| 家庭成员识别 | 注册家庭成员人脸，实时识别并标注身份 |
| 人员统计 | 统计当前画面/家中人数，区分熟人与陌生人 |
| 陌生人告警 | 未注册人脸出现即触发告警 |
| 危险区域 | 可配置禁区（如厨房禁止小孩进入），闯入即告警 |
| 异常检测 | 积水、着火、人员摔倒等紧急情况识别（≥2 种） |
| 告警管理 | 统一告警展示、处置、日志与事件回放 |

---

## 3. 总体架构

### 3.1 架构概览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     展示层（Vue3 + Vite + Element Plus）                 │
│  居家监控(WebRTC+Canvas) | 家人录入 | 危险区域 | 告警中心 | 事件记录      │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │ HTTP / REST / JWT
┌───────────────────────────────▼─────────────────────────────────────────┐
│                  Django 4.2 + DRF 单体后端（:8000 / :8010）               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ accounts │ │households│ │  zones   │ │  alerts  │ │  events  │      │
│  │ 认证/用户 │ │ 家庭隔离  │ │ 危险区域  │ │ 告警管理  │ │ 事件日志  │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────────┐ ┌────────────┐ ┌──────────┐ ┌─────────┐ │
│  │   home   │ │ video_stream │ │    face    │ │ detection│ │notificat│ │
│  │ 人数统计  │ │ RTSP→AI→MJPEG│ │ 人脸/活体   │ │ 区域/异常 │ │ 钉钉通知 │ │
│  └──────────┘ └──────────────┘ └────────────┘ └──────────┘ └─────────┘ │
│                    Swagger: /api/docs/                                   │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │ RTSP 拉流
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
       ┌────────────┐   ┌────────────┐   ┌────────────┐
       │   MySQL    │   │  截图/文件  │   │  MediaMTX  │
       │  业务数据   │   │  告警快照   │   │ RTMP :9090 │
       └────────────┘   └────────────┘   │ RTSP :8554 │
                                          │WebRTC :8889│
                                          └──────┬─────┘
                                                 │ RTMP 推流
                                          ┌──────▼──────┐
                                          │ 家用摄像头   │
                                          │ 手机 / OBS  │
                                          └─────────────┘

展示层视频：WebRTC iframe（:8889）+ FaceOverlay Canvas（轮询 /api/video/presence/）
```

### 3.2 架构设计原则

| 原则 | 说明 |
|------|------|
| Django 单体集成 | 业务 API 与 AI 模块均为 `backend/apps/` 下的 Django App，同进程运行 |
| 告警服务化 | AI 模块通过 `apps.alerts.services.create_alert()` 写库（亦可 HTTP POST `/api/alerts/`） |
| 推拉流分离 | MediaMTX 接收 RTMP 推流并转发 RTSP，Django OpenCV 拉流分析 |
| 多家庭隔离 | 用户可加入多个家庭，前端携带 `X-Active-Household-Id` 请求头 |
| 前后端分离 | 前端通过 REST API 访问业务层；监控页用 WebRTC 预览 + overlay API 展示 AI 标注 |

### 3.3 架构演进说明

| 阶段 | 方案 | 状态 |
|------|------|------|
| v1.0 | Flask 单体（AI + 业务同进程） | 已废弃 |
| v1.2 | Django 业务层 + AI 模块分 feature 分支开发 | 已合并至 `dev` |
| v1.3 | Django 单体集成（`face` / `detection` / `video_stream` 均已合入） | 已合并 |
| v1.5 | WebRTC 主预览 + 后端 overlay API；音频检测；活体反欺骗；Worker 线程安全 | **当前方案** |

> **联调状态（2026-07-12）**：AI 处理链、WebRTC 预览 + Canvas 叠加、告警中心端到端验收已完成；`ai-video-integration` 已归档。进行中：`audio-abnormal-sound-detection` 待归档。

---

## 4. 技术选型

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue3 + Vite + Element Plus + Vue Router | 监控大屏、区域画框、告警列表 |
| 业务后端 | Django 4.2 + DRF + SimpleJWT | 认证、CRUD、Swagger 文档 |
| API 文档 | drf-yasg | `/api/docs/` 自动生成 |
| AI 运行环境 | conda `home-camera`（Python 3.10） | Windows 上 dlib 必须通过 conda 安装 |
| 人脸识别 | dlib + face_recognition | 128 维编码，家人库比对 |
| 行人检测 | YOLOv8n（优先）+ OpenCV HOG 降级 | 危险区域闯入 / 接近 / 逗留 |
| 异常检测 | OpenCV HSV / 形态分析 + 多信号融合 | 积水、着火、跌倒 |
| 音频检测 | PANNs CNN14 + FFmpeg 采集 | 尖叫/争吵/玻璃破碎等（可选加分） |
| 数据库 | MySQL 8.x | 用户、家庭、区域、告警、事件 |
| 流媒体 | MediaMTX（Docker） | 替代原 Nginx-RTMP 方案，RTMP 入 / RTSP 出 |
| 部署 | gunicorn + Nginx 反代 | 生产环境 API 与静态资源 |

---

## 5. 推拉流方案

| 环节 | 协议 | 端口 | 说明 |
|------|------|------|------|
| 推流 | RTMP | 9090 | 摄像头/手机/OBS → MediaMTX |
| 中转 | RTSP | 8554 | MediaMTX 内部转发 |
| WebRTC 预览（主） | HTTP | 8889 | 前端 HomeMonitor / ZoneEditor iframe 低延迟画面 |
| 拉流 + AI | OpenCV `VideoCapture` | — | Django `video_stream` 采集/处理线程分离 |
| overlay API | HTTP | 8010 / 8000 | `/api/video/presence/` 提供 AI 框数据 |
| MJPEG（备用） | HTTP | 8010 / 8000 | `/video_feed/{id}` 后端烧录标注，调试用 |

```
推流：rtmp://{IP}:9090/stream/1
拉流：rtsp://127.0.0.1:8554/stream/1
WebRTC（前端主预览）：http://{IP}:8889/stream/1/
overlay API：GET /api/video/presence/?stream_id=1
MJPEG（后端备用）：http://{IP}/video_feed/1
```

### 流 ID 双轨约定

视频层（MediaMTX / OBS 推流码）与业务层（zones / alerts / events）使用不同 ID，前端 `constants/streams.js` 负责映射：

| 场景 | 视频流 ID | 业务 stream_id |
|------|-----------|----------------|
| 客厅 | `1` | `living_room` |
| 厨房 | `2` | `kitchen` |

### 视频处理流水线（已实现）

```
RTSP 帧 → CameraWorker（采集/AI 分线程，frame.copy() 防并发）
    → 跳帧（FRAME_SKIP）
    → process_frame() 处理器链：
        ① face.process_frame()      — 人脸检测/识别/画框/人数统计/活体
        ② detection.process_frame() — YOLO/HOG 行人 + 禁区/接近/逗留 + 积水/着火/跌倒
        ③ detection.draw_overlays() — 区域多边形
        ④ face.draw_face_boxes()    — 人脸框最终绘制
    → 发布 overlay 快照 → /api/video/presence/
    → MJPEG 编码输出（/video_feed/，含烧录标注）
    → create_alert() 写入告警表 + 事件快照/短视频
```

前端 `HomeMonitor.vue` / `ZoneEditor.vue`：WebRTC iframe 播放原始画面，`FaceOverlay.vue` 每 200ms 轮询 `/api/video/presence/` 在 Canvas 上叠加人物/人脸/异常框。

---

## 6. 功能架构

### 6.1 功能模块图

```
                    ┌─────────────────┐
                    │ 用户/家庭/权限    │
                    │ (JWT + Household)│
                    └────────┬────────┘
                             │
    ┌────────────────────────┼────────────────────────┐
    ▼                        ▼                        ▼
┌─────────┐           ┌─────────────┐          ┌─────────────┐
│ 人脸识别  │           │  危险区域    │          │  异常检测    │
│ 家人注册  │           │  厨房禁区    │          │  积水       │
│ 身份识别  │           │  小孩闯入    │          │  着火       │
│ 人数统计  │           │  闯入告警    │          │  摔倒       │
│ 陌生人   │           │             │          │             │
└────┬────┘           └──────┬──────┘          └──────┬──────┘
     └───────────────────────┼────────────────────────┘
                             ▼
                    ┌─────────────────┐
                    │  告警中心 (Django) │
                    └─────────────────┘
```

### 6.2 核心业务场景

| 场景 | 触发条件 | 系统行为 | 告警类型 |
|------|----------|----------|----------|
| 家人识别 | 已注册家庭成员出现 | 绿框 + 显示姓名 | — |
| 陌生人 | 未注册人脸 | 红框 + 告警 | `FACE_UNKNOWN` |
| 人数统计 | 画面中多张人脸 | 更新在家人数/陌生人数 | 前端轮询展示 |
| 小孩进厨房 | 角色=child 进入厨房禁区 | 告警 | `INTRUSION` |
| 距边缘过近 | 禁区外 safe_distance 内 | 告警 | `PROXIMITY` |
| 异常停留 | 禁区附近 dwell_time 超时 | 告警 | `LOITER` |
| 积水 | 画面低处大面积蓝色/反光区域 | 告警 | `WATER` |
| 着火 | 高亮/红色火焰颜色区域 | 告警 | `FIRE` |
| 摔倒 | 人体框高宽比异常（躺倒） | 告警 | `FALL` |

### 6.3 与验收清单对应

| 验收模块 | 分值 | 本系统实现 |
|----------|------|------------|
| 人脸识别 | 12 | 家人注册 + 识别 + 陌生人告警 + 存储人脸信息 |
| 目标检测 | 25 | 危险区域画框 + 厨房禁止小孩进入 + 闯入告警 |
| 实时视频检测 | 20 | 积水 + 着火 + 摔倒（3 种异常/紧急情况） |
| 告警中心 | 8 | 展示 + 处置 + 日志 + 回放 |
| 项目基础 | 3 | GitHub 分支/Network/Contributors |
| 文档 | 10 | 日报 + 设计文档 + 演示视频 |

---

## 7. 项目结构

### 7.1 仓库目录

```
home-camera-monitor/
├── frontend/              # Vue3 前端 (:5173)
│   └── src/
│       ├── views/         # 页面组件
│       ├── components/    # 公共组件（PersonStats 等）
│       ├── api/           # Axios 接口封装
│       ├── constants/     # streams.js 流 ID 映射
│       └── router/        # Vue Router
├── backend/               # Django 后端 (:8000)
│   ├── config/            # settings / urls / wsgi
│   ├── apps/              # 业务 + AI Django Apps（见 7.2）
│   ├── dat/               # dlib 模型说明
│   ├── environment.yml    # conda 环境（Python 3.10 + dlib）
│   └── requirements.txt
├── nginx/                 # Nginx / MediaMTX 部署说明
├── deploy/                # 部署脚本（见 deploy/README.md）
├── .github/workflows/     # GitHub Actions CI
├── Jenkinsfile            # Jenkins Pipeline（CD）
├── openspec/              # OpenSpec 规格驱动开发
└── docs/                  # 项目文档（见 docs/README.md）
```

### 7.2 后端 Django Apps

```
backend/apps/
├── accounts/              # 用户认证（手机号 + JWT）、家庭成员 CRUD
│   ├── models.py          # User, FamilyMember, SmsVerificationCode, Captcha
│   └── management/commands/seed_demo_data.py
├── households/            # 家庭管理、成员关系、加入申请、摄像头
│   ├── models.py          # Household, HouseholdMembership, JoinApplication, Camera
│   ├── middleware.py      # 活跃家庭上下文
│   └── filters.py         # resolve_active_household_id()
├── home/                  # 居家监控
│   └── views.py           # GET /api/home/presence/
├── zones/                 # 危险区域 CRUD
├── alerts/                # 告警列表 / 创建 / 处置
│   └── services.py        # create_alert() 供 AI 模块调用
├── events/                # 事件日志
├── video_stream/          # 视频拉流 + AI 处理 + MJPEG 输出（B-苏哲勋）
│   ├── services.py        # CameraWorker、process_frame()、overlay 快照
│   ├── views.py           # /video_feed/、/api/video/status、/api/video/presence/
│   └── clips.py           # 告警短视频录制
├── face/                  # 人脸识别 + 活体反欺骗（C-王梓铭）
│   ├── services.py        # FaceRecognitionService
│   ├── liveness.py        # 被动活体检测
│   ├── views.py           # /api/face/register|analyze|liveness/
│   └── tests/
├── detection/             # 危险区域 + 异常 + 音频检测（D-李东礼）
│   ├── services.py        # DetectionService（YOLO/HOG + 禁区/异常）
│   ├── audio_service.py   # PANNs 音频分类
│   ├── audio_capture.py   # FFmpeg 音频采集
│   ├── av_correlation.py  # 音视频联动 EMERGENCY
│   └── tests/
├── notifications/         # 钉钉通知 + 告警升级链（E-刘帅华）
│   ├── views.py           # /api/notifications/config/
│   ├── signals.py         # 告警推送
│   └── checker.py         # 超时逐级上报
└── reports/               # AI 监控日报（A/E）
    └── services.py        # 日报生成（模板 / 可选 LLM）
```

### 7.3 模块职责

| 模块 | 职责 | 负责人 |
|------|------|--------|
| `accounts` | 手机号注册/登录、JWT、家庭成员 CRUD | 刘帅华 |
| `households` | 家庭创建/加入/审核、摄像头管理、多家庭隔离 | 刘帅华 |
| `home` | 人数统计 API（读取 face 服务快照） | 王梓铭 / 刘帅华 |
| `zones` | 危险区域 CRUD（多边形 + forbidden_roles） | 刘帅华 |
| `alerts` | 告警管理、create_alert 服务层 | 刘帅华 |
| `events` | 事件日志 | 刘帅华 |
| `video_stream` | RTSP 拉流、AI 处理链、overlay API、MJPEG 备用输出 | 苏哲勋 |
| `face` | 人脸注册/识别/活体反欺骗/人数统计/陌生人告警 | 王梓铭 |
| `detection` | 禁区闯入、积水/着火/跌倒、音频异常、音视频联动 | 李东礼 |
| `notifications` | 钉钉 Webhook 推送、告警升级链 | 刘帅华 |
| `reports` | AI 监控日报生成 | 刘帅华 / 牛雨昊 |

### 7.4 API 路由一览

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/auth/login/` | POST | JWT 登录 |
| `/api/auth/register/` | POST | 手机号注册 |
| `/api/auth/members/` | CRUD | 家庭成员管理 |
| `/api/auth/me/` | GET | 当前用户信息 |
| `/api/households/` | CRUD | 家庭管理 |
| `/api/households/join/` | POST | 申请加入家庭 |
| `/api/cameras/` | CRUD | 摄像头管理 |
| `/api/home/presence/` | GET | 当前人数统计 |
| `/api/zones/` | CRUD | 危险区域 |
| `/api/alerts/` | GET/POST | 告警列表 / 创建 |
| `/api/alerts/{id}/handle/` | PUT | 处置告警 |
| `/api/events/` | GET/POST | 事件日志 |
| `/api/face/register/` | POST | 人脸注册（多帧活体校验） |
| `/api/face/liveness/` | POST | 多帧活体检测 |
| `/api/face/analyze/` | POST | 单帧人脸分析（调试用） |
| `/api/detection/analyze/` | POST | 单帧异常检测（调试用） |
| `/api/detection/status/` | GET | 检测服务状态 |
| `/api/detection/audio/status/` | GET | 音频检测状态 |
| `/api/video/presence/` | GET | 实时 overlay + 人数统计 |
| `/video_feed/{id}` | GET | MJPEG 备用流（含后端烧录标注） |
| `/api/video/status` | GET | 视频 worker 状态 |
| `/api/notifications/config/` | GET/PUT | 钉钉通知配置 |
| `/api/video/streams/{id}/source` | GET | 推流/拉流地址信息 |
| `/api/reports/daily/` | GET/POST | AI 监控日报 |
| `/api/snapshots/{filename}/` | GET | 事件回放截图 |
| `/api/docs/` | GET | Swagger 文档 |

---

## 8. 前端模块设计

> **负责人：牛雨昊（A）**

```
frontend/src/
├── views/
│   ├── Login.vue              # 登录
│   ├── Register.vue           # 注册
│   ├── HomeMonitor.vue        # 居家监控（WebRTC iframe + FaceOverlay）
│   ├── FamilyRegister.vue     # 家庭成员人脸录入（多帧活体）
│   ├── ZoneEditor.vue         # 危险区域画框（WebRTC + Canvas 640×480）
│   ├── AlertCenter.vue        # 告警中心
│   ├── EventLog.vue           # 事件记录（含回放）
│   ├── DailyReport.vue        # AI 监控日报
│   ├── UserManage.vue         # 用户管理
│   ├── HouseholdManage.vue    # 家庭管理（切换活跃家庭）
│   ├── Profile.vue            # 个人信息
│   └── NotificationSettings.vue # 钉钉通知配置
├── components/
│   ├── FaceOverlay.vue        # WebRTC 上 Canvas 叠加 AI 框
│   ├── PersonStats.vue        # 人数统计面板
│   └── FaceCapture.vue        # 本机摄像头多帧采集
├── constants/
│   └── streams.js             # 视频流 ID ↔ 业务 stream_id 映射
├── router/
│   └── index.js               # 路由 + JWT 守卫
└── api/
    ├── index.js               # 接口定义
    └── request.js             # Axios 封装（JWT / X-Active-Household-Id）
```

### 前后端交互

| 功能 | 接口 |
|------|------|
| 登录 | POST `/api/auth/login/` |
| 视频预览 | WebRTC `http://{IP}:8889/stream/{id}/`（iframe） |
| AI 叠加 | GET `/api/video/presence/?stream_id={id}`（200ms 轮询） |
| 家人注册 | POST `/api/face/register/`（含多帧活体） |
| 人数统计 | `PersonStats` 读取 presence 快照 |
| 危险区域 | CRUD `/api/zones/` |
| 告警 | GET/PUT `/api/alerts/` |
| 事件记录 | GET `/api/events/` |
| 家庭管理 | CRUD `/api/households/` |
| 钉钉通知 | GET/PUT `/api/notifications/config/` |

> Django 路由要求尾部斜杠 `/`，前端 Axios 已统一适配。活跃家庭通过请求头 `X-Active-Household-Id` 传递。

---

## 9. 数据库设计

### accounts_user — 用户

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| phone | VARCHAR(16) UNIQUE | 手机号（登录凭证） |
| password | VARCHAR | 密码哈希 |

### household — 家庭

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| name | VARCHAR(64) | 家庭名称 |
| created_by | FK → User | 创建者 |

### family_member — 家庭成员

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| household_id | FK → Household | 所属家庭 |
| name | VARCHAR(64) | 姓名 |
| role | VARCHAR(16) | adult / child / elder / guest |
| student_id | VARCHAR(32) | 学号/工号（可选） |
| face_encoding | JSON | 128 维特征向量 |
| is_active | BOOL | 是否启用 |

### zone — 危险区域

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| household_id | FK → Household | 所属家庭 |
| name | VARCHAR(64) | 如「厨房」 |
| stream_id | VARCHAR(32) | living_room / kitchen |
| points_json | JSON | 多边形坐标 |
| forbidden_roles | JSON | 禁止进入的角色，如 `["child"]` |
| is_active | BOOL | 是否启用 |

### alert — 告警

| type | 含义 |
|------|------|
| `FACE_UNKNOWN` | 陌生人 |
| `FACE_SPOOF` / `FACE_REPLAY` / `FACE_DEEPFAKE` | 活体反欺骗攻击 |
| `INTRUSION` | 危险区域闯入 |
| `PROXIMITY` | 距离过近 |
| `LOITER` | 异常停留 |
| `WATER` | 积水 |
| `FIRE` | 着火 |
| `FALL` | 摔倒 |
| `SCREAM` / `FIGHT` / `CRYING` / `GLASS_BREAK` | 音频异常 |
| `EMERGENCY` | 音视频联动紧急事件 |

### event_log — 事件日志

| 字段 | 类型 | 说明 |
|------|------|------|
| household_id | FK → Household | 所属家庭 |
| event_type | VARCHAR(32) | 事件类型 |
| stream_id | VARCHAR(32) | 视频流 ID |
| description | VARCHAR(256) | 事件描述 |
| metadata | JSON | 附加数据 |
| created_at | DATETIME | 发生时间 |

---

## 10. 关键业务流程

### 10.1 人脸识别与人数统计

```
RTSP 视频帧 → video_stream.process_frame()
    → face.services.process_frame()
    → 检测所有人脸 → 与家人员比对
    → 统计：total / family / stranger
    → 更新 overlay 快照 → GET /api/video/presence/ 可读
    → 陌生人 → create_alert(FACE_UNKNOWN)
    → MJPEG 备用输出（含烧录标注）
    → 前端 WebRTC + FaceOverlay Canvas 展示
```

### 10.2 危险区域（厨房禁小孩）

```
zone 配置：厨房多边形 + forbidden_roles=["child"]
    → detection 服务 YOLOv8n 行人检测（HOG 降级）
    → 结合 face 识别的 role + 行人进入多边形
    → create_alert(INTRUSION)
```

### 10.3 异常检测（积水/着火/跌倒）

```
每 N 帧在 process_frame() 链中执行：
    WATER  — 画面下方 HSV 蓝色/反光大面积 → create_alert(WATER)
    FIRE   — 高亮红/黄火焰颜色区域超阈值 → create_alert(FIRE)
    FALL   — 人体框高/宽比低于阈值（躺倒）→ create_alert(FALL)
```

---

## 11. 部署架构

```
Linux 云服务器（152.136.29.158）
├── MediaMTX（Docker）            # RTMP :9090 / RTSP :8554 / WebRTC :8889
├── Django + gunicorn (:8010)     # 视频拉流 + AI 处理 + API（systemd home-camera-backend）
├── MySQL                         # 业务数据库
├── Nginx 反代 (:80)              # 前端静态 + /api/、/video_feed/ → 8010
├── Jenkins (:8080)               # CI/CD（可选，结题用）
└── backend/venv 或 conda         # dlib + face_recognition
```

本地开发详见 [DEV_SETUP.md](../开发指南/DEV_SETUP.md)；云部署详见 [B组-云部署与联调指引.md](../部署运维/B组-云部署与联调指引.md)；CI/CD 详见 [CI-CD使用说明.md](../部署运维/CI-CD使用说明.md)。

---

## 12. 团队分工

| 代号 | 成员 | 角色 |
|------|------|------|
| A | 牛雨昊 | 组长 + 前端 + Git/部署 |
| B | 苏哲勋 | 流媒体 + 视频拉流 |
| C | 王梓铭 | AI 人脸识别 + 人员统计 |
| D | 李东礼 | AI 危险区域 + 异常检测（积水/着火/跌倒） |
| E | 刘帅华 | Django 业务 + 数据库 + Swagger |
| F | 刘澎潮 | 专职文档 |

**代码仓库**：https://github.com/niu-spec/home-camera-monitor

**Git 分支规范**：

| 分支 | 用途 |
|------|------|
| `main` | 稳定发布 |
| `dev` | 集成分支（当前开发主线） |
| `feature/frontend` | 前端开发 |
| `feature/business` | Django 业务后端 |
| `feature/face` | 人脸识别 |
| `feature/detection` | 异常检测 |
| `feature/nginx` | 流媒体 / MediaMTX |
| `feature/infra` | CI/CD、环境脚本 |

各 feature 分支开发完成后合并至 `dev`，联调通过后合并至 `main`。

---

## 13. 演示脚本（结题视频）

1. 登录并切换至演示家庭，注册爸爸、妈妈、小孩三名家庭成员（多帧活体）  
2. OBS 推流至客厅摄像头，监控页 WebRTC 画面 + Canvas AI 标注  
3. 客厅画面：PersonStats 显示当前人数  
4. 陌生人入镜 → 陌生人告警（告警中心可回放截图/短视频）  
5. 小孩进入厨房禁区 → 区域闯入告警  
6. 模拟积水/着火/摔倒画面 → 对应告警  

---

*文档结束 — 变更请通知文档专员刘澎潮（F）。*
